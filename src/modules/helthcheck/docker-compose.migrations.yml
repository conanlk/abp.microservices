version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Helthcheck_Identity
      - Helthcheck_DB=Helthcheck_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
